{
  "name": "graphql-devtools",
  "version": "1.3.0",
  "private": true,
  "description": "A GraphQL panel in Chrome DevTools: every operation the page sends, with its variables, response, errors and headers",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "build": "node scripts/build.js",
    "test:ci": "node scripts/test-report.js"
  },
  "engines": {
    "node": ">=20"
  }
}
